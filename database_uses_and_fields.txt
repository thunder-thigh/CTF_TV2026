Database Fields:

auth_table:
team_name TEXT NOT NULL;
password TEXT NOT NULL;				//This needs to be hashed before entry

teams_table:							//Checks with team password before registration
									//Updatable with known password
team_name TEXT NOT NULL PRIMARY KEY ;
leader_enrollment TEXT NOT NULL;
leader_name TEXT NOT NULL;
leader_email TEXT NOT NULL;
mem1_enroll TEXT;
mem1_name TEXT;
mem1_email TEXT;
team_leader_email TEXT;
mem2_name TEXT;
team_leader_name TEXT;
team_leader_email TEXT;
mem3_name TEXT;
team_leader_name TEXT;
team_leader_email TEXT;

progress table:
team_name TEXT NOT NULL;
problems_column_<1 to x> INTEGER NOT NULL DEFAULT;
team_progress INTEGER;
#One row for storing marks per question	